# Contexto para agentes (Veta)

- **Skills (Agent Skills):** `.agents/skills/` — cada skill es una carpeta con `SKILL.md`. Índice del equipo: `.agents/skills/README.md`.
- **Reglas Cursor:** `.cursor/rules/` — reglas `.mdc` (siempre aplicadas o por globs).
- **Estado del producto y restricciones:** `memory-bank/`.

Más detalle sobre la carpeta `.agents/`: `.agents/README.md`.
