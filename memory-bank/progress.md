# Progress

**Estado actual y WIP:** ver `activeContext.md`. Este archivo es un resumen de lo completado y pendiente.

## Completado (resumen por áreas)

- **Infra y auth:** Next.js 16 (App Router), Docker, Supabase (schema, RLS, Auth). Login/signup, magic links, sesión, caducidad con redirección. Migración desde Vite.
- **CRUD:** Clientes, proveedores, catálogo (productos), proyectos. Layout app (sidebar, responsive). Eliminación de proyectos desde la lista (menú ⋮ en cada card, confirmación con `ConfirmDeleteDialog`; RLS solo permite borrar los propios).
- **Detalle de proyecto:** Info general, espacios, renders por espacio, presupuesto (ítems, partidas por fase/categoría, edición, exclusión), órdenes de compra (gestionables, cobertura), **pagos** (registro por proyecto, tipos, vinculación a PO/costes), costes adicionales, documentos (URL + subida), notas (archivado).
- **Almacenamiento:** Tabla `assets`, Backblaze B2, rutas por usuario/proyecto. Subida de imágenes (producto, espacios, documentos). Sharp, límites, `user_storage_usage`.
- **Planes:** Base / Pro / Studio. Consumibles y modalidades, `plan-copy.ts`, `plan-capability.ts`, restricciones por plan, cambio de plan en `/settings/plan`.
- **Marca y marketing:** Veta, logos light/dark, tipografía Montserrat. Páginas marketing (home, about, pricing, contact, planes), SEO, sitemap, robots, OG, MailerSend contacto, dominio veta.pro.
- **i18n (multiidioma) + SEO fuerte (next-intl):** `src/i18n/routing.ts` como fuente de verdad para slugs (ES default sin prefijo, EN con `/en`) usando `localePrefix: "as-needed"` y `pathnames` para `/pricing|/about|/contact` y landings de planes. Metadatos por página con `generateMetadata` configurando `alternates.canonical`, `alternates.languages` (`es`, `en`, `x-default`) y `openGraph.url` consistentes. `src/app/sitemap.ts` lista variantes ES/EN e incluye solo páginas indexables. Dev-only `test-error` con `layout.tsx` server y `robots: { index: false, follow: false }`. Fix de navegación: `src/app/page.tsx` envuelve la home del root con el mismo `MarketingLayout` que el resto de `(marketing)` para que navbar/footer estén presentes. `LanguageToggle` debe navegar a la URL canónica correcta para ES default (evitar rutas inválidas como `/es/precios`).
- **UI/UX y a11y:** Tema natural/pastel, dark mode OKLCH, animaciones (Framer Motion), skeletons, skip link, una h1 por página, focus-visible, `prefers-reduced-motion`, loading por ruta.
- **PDF:** Presupuestos con `@react-pdf/renderer` (espacios, costes adicionales, opciones de filtrado). Para planes basic/plus: cabecera con logo Veta (light) y nombre, pie en cada página "Document generated with Veta - www.veta.pro" con logo en miniatura (issue #95).
- **Cuenta:** Perfil, personalización (/customization), Mi cuenta (email, zona peligro, eliminar cuenta, cambiar email). Legal (términos, privacidad, **Cookies**: explicación, necesarias/Supabase, estadísticas/GA4-GTM-Vercel, personalización, marketing, no clasificadas). Plantillas email Supabase.
- **Seguridad:** Headers (CSP, X-Frame-Options, etc.), validación ownership en uploads, OWASP documentado.
- **Analytics:** GTM, GA4, gestión de cookies propia (banner + Consent Mode v2), CSP ajustada para terceros.
- **PageSpeed / marketing:** Auditoría PageSpeed sobre rutas (marketing); preconnect a GTM en layout; secciones below-the-fold de la home lazy-load con `next/dynamic` (`_sections/`); contraste WCAG AA (globals.css); logo con `alt=""` cuando va con wordmark; docs `pagespeed-audit-marketing.md`, `pagespeed-improvements-from-report.md`.
- **Contraste modo claro y oscuro (rama fix/dark-mode-contrast-and-cards):** Modo oscuro con paleta marrón (bg 0.17, card 0.18, secondary/accent 0.17), textos 0.98, brand-foregrounds oscuros; modo claro con contraste ≥4.5:1 (textos 0.16–0.17, brand-foregrounds oscuros), profundidad por capas. Botón primary con texto blanco (0.98) por decisión de diseño (ratio ~1.5:1). **Tema por defecto:** `system` (modo claro salvo que el navegador indique oscuro). **Dark:** sombras invertidas (luz desde arriba), glow-dark en CTAs, pestaña activa en proyectos resaltada (bg-card, ring, shadow). Docs `contrast-audit.md` actualizado.
- **Otros:** Moneda por proyecto/producto, validación teléfono (libphonenumber), mensajes auth amigables, Supabase keys por entorno.
- **Reporte de fallos:** Enlace "Reportar fallo" en el menú de usuario que abre GitHub Issues con plantilla YAML (ES/EN), pre-rellenando título y URL de la vista. Ver `src/lib/report-bug.ts` y `.github/ISSUE_TEMPLATE/`.
- **Formulario de contacto (anti-spam y trazabilidad):** Honeypot (`website`), comprobación de tiempo (mín. 4 s, máx. 1 h con `_ts`), rate limiting (5 envíos/IP/minuto en middleware y en server action), límites de longitud (nombre 50, email 254, asunto 100, mensaje 5000). IP del envío incluida en el correo (texto y HTML). Contador de caracteres solo en el campo Mensaje; textarea redimensionable en vertical (`resize-y`). Ver `src/app/(marketing)/contact/`, `src/lib/rate-limit.ts` (grupo `contact`, `getClientIpFromHeaders`).
- **Onboarding (issue #51):** Wizard por pasos: bienvenida (sessionStorage, sin campo en BD), config → cliente → proyecto → public-profile (PRO/STUDIO). Sin pasos espacio ni catálogo. Modal un paso por sesión; "Más tarde" en sessionStorage; CTA cierra modal y navega con `?onboarding=stepId`; resaltado en destino (hook `ready` tras loading). Checklist en dashboard. Correo público = `profile.email`. Personalización en `/settings/customization`; diálogo nuevo proyecto usa valores por defecto de cuenta. Ver `src/lib/onboarding.ts`, `src/components/onboarding/`, `src/lib/use-onboarding-status.ts`, `src/lib/use-onboarding-highlight.ts`.
- **Vistas de configuración:** Sidebar unificado (Cuenta, Personalización, Tu plan, Tema); breadcrumbs en vistas de configuración; Perfil integrado en Cuenta (nombre/empresa); menú cuenta unificado (avatar colapsado/expandido); oferta plan cambio (0 €, precio tachado, "Oferta por tiempo limitado"); selector mensual/anual oculto; input estándar `INPUT_CONFIG_STANDARD_CLASS` en configuración; iconos en labels (personalización, cuenta).
- **Memory bank:** Optimización aplicada: activeContext, productContext, constraints, progress; eliminados techContext, systemPatterns y archivos duplicados (.optimized). Ver OPTIMIZATION_PROPOSAL.md.
- **Rendimiento (rama fix/web-performance):** Tema por defecto `system`; sombras/glow invertidos en dark y pestaña activa resaltada; `optimizePackageImports` para lucide-react; `.below-the-fold` (content-visibility) en home; middleware excluye `_vercel` (scripts Vercel sin redirect a sign-in, caché correcta); Cache-Control immutable para `/_next/static/*`; scroll tab proyecto con requestAnimationFrame (evitar forced reflow); tests de headers usan helper para config `/:path*`.
- **Cuenta demo (issues #76 y #94):** Cuenta `demo@veta.pro` con plan Studio; bloqueo de escritura (create/update/delete) vía triggers en BD y guard 403 en API (uploads, delete account); mensaje amable `DEMO_ACCOUNT_READ_ONLY` en frontend (`getDemoAccountMessage` en diálogos y páginas de mutación). Script reejecutable `scripts/seed-demo-account.ts` (npx tsx, SUPABASE_URL + SUPABASE_SERVICE_ROLE_KEY) para crear/resetear usuario demo y seed (3 clientes, 7 proveedores, 2 proyectos con espacios, ítems, honorarios, ÓC, gastos, pagos, notas). Vista `/demo`: formulario con email del visitante; API `POST /api/auth/demo-request` genera magic link para demo@veta.pro y lo envía al correo del visitante (MailerSend), y notifica a CONTACT_EMAIL_TO. Ver plan en .cursor/plans y `scripts/README.md`.
- **Rama feat/demo-account-seed-and-ui:** Seed demo ampliado: clientes con nombres ES/EN/PT y datos completos; proveedores 50% empresas/50% independientes; 7 productos con imágenes (chaise longue, sofá, silla, mesa, escritorio, lámpara, alfombra); espacios Despacho/Salón con imágenes; partidas de presupuesto (honorarios, obra, operativo) e is_internal_cost explícito; proyecto completado en MXN con honorarios visibles y pagos (fees + 2 ÓC); proyecto activo en EUR con fechas inicio/fin; perfil demo "Veta Demo" / demo@veta.pro. UI: Presupuesto muestra partidas (fix query is_internal_cost null); desviación 0% en verde; totalizaciones en negro; Card Mobiliario con borde; Renders disponibles con documents basic; /auth/complete con Suspense para build. Migración STUDIO documents=full y script verify-demo-account + SEED-DEMO-EXPECTED.md.
- **Rama fix/demo-account-bugs (UI y copy):** Espacios: primera imagen de renders como fondo de tarjeta con velo beige 75% y opacidad fondo 85%; glow en título y descripción; catálogo: imagen de producto como fondo (cover, center) en card; clientes: menú acciones flotante bottom-right y menú hacia arriba (side=top); presupuesto: sección Mobiliario y Productos primero; pagos: resumen total pendiente/recibido, barra de progreso cobro; órdenes de compra: título "Órdenes de compra y fabricación", estados Recibida=primary y Confirmada=púrpura, seed con status draft; botón menú móvil 48×48, logo escalado, rounded-full; tipado reduce en project-payments.
- **Área pública ampliada (issues #104, #105, #106, #107):** Vista de costes del proyecto (presupuesto por fases/categorías, sin partidas internas, mismo contenido que PDF), enlace "Costes del proyecto". Vista de pagos (historial read-only, mismos datos que vista privada). Vista de productos (ítems no excluidos, precio venta y total, estado, sin precio unitario); enlace "Productos". En listado de productos (catálogo app y vista pública): clic en la foto abre lightbox con imagen en grande. RPCs: get_project_public_budget, get_project_public_payments, get_project_public_products, get_project_public_currency. Rutas /view-project/[token]/costs, payments, products (en inglés). **Seguridad:** rate limit 120 req/min por IP en /view-project/*; maskShareToken/maskViewProjectPath para no loguear el token; redirect post-login validado (getSafeRedirectPath, sin open redirect). Redirecciones permanentes de rutas en español (productos, costes, pagos) a las rutas en inglés.
- **Rama feat/public-project-spaces-documents:** Vista pública "Productos" renombrada a **Espacios** (`/view-project/[token]/spaces`): espacios colapsables, miniaturas 100×100 de renders (lightbox), productos debajo; solo se muestran bloques con ítems. Nueva vista **Documentos** (`/documents`) en grid 4 columnas con icono y nombre, enlace al archivo. RPCs `get_project_public_space_images` (URL desde `space_images` o `assets`) y `get_project_public_documents`. Enlace en home pública a Espacios y Documentos. i18n `app-view-project` (spaces, documents, spacesTabEmpty). **Compartir enlace:** `project-share` dividido en `project-share-actions.ts` (`use server`) + `project-share-types.ts` para evitar fallos HMR/Turbopack desde el diálogo cliente. **Tema (view-project shell):** `ThemeToggleSimple` usa `resolvedTheme`; icono = modo al que cambiar (luna en claro, sol en oscuro); script inicial en `layout` raíz hace `classList.toggle('dark')` y quita `light`. **next.config:** eliminadas redirecciones `/view-project` ES→EN (productos/costes/pagos/products). Tests rate-limit actualizados a `/spaces`.
- **Enlace único y vista cliente (issues #74 y #75):** En `projects`: columnas `token` (text, unique) e `is_public_enabled` (boolean). Migración rellena token para proyectos existentes. Botón "Vista pública" (ojo abierto/cerrado según estado) abre modal con URL, "Renovar enlace" y "Publicar/Restringir acceso". Ruta pública `/view-project/[token]` sin autenticación muestra: nombre, arquitecto/a, fase, fechas. Layout `view-project` con `robots: { index: false, follow: false }`. Función RPC `get_project_share_by_token` (SECURITY DEFINER) lee de `projects`. Middleware: `pathname.startsWith("/view-project/")` en rutas públicas.
- **TabSectionHeader (rama fix/tab-section-header-margin):** Margen inferior reducido a la mitad en todas las pestañas de proyecto (-mb-3).
- **View-project shell y h1 (rama feat/view-project-h1-and-shell-consistency):** Mismo estilo de h1 en Costes, Pagos y Productos (text-foreground text-xl font-semibold sin mb-4). Shell con wrapper max-w-4xl space-y-4 para consistencia.
- **Ajustes UI view-project y veta-app (rama feat/view-project-public-and-security):** Vista costes: orden columnas (Descripción, Partida, Importe), columna Fase eliminada; tabla productos (Producto, Espacio primero); subtotal con pt-1 y texto footer a la izquierda; partida/producto sin negrita. Vista pagos: columnas Fecha, Referencia, Descripción, Monto (Tipo eliminado). Productos públicos: altura de la imagen en función de la tarjeta (Card flex min-h, contenido flex-1, botón h-full). Tooltips en veta-app con variante tertiary (TooltipContent: default, secondary, tertiary). Espacios: botones Productos/Renders solo icono y flex-1 en la misma línea. Diálogo productos del espacio: grid 4 columnas desde sm. Progress actualizado.
- **Rama feat/minor-features:** PDF presupuesto: datos Cliente y Arquitecto/a en dos columnas con títulos; cuadros de productos (Mobiliario y Productos) ancho completo; columna vacía al final de tablas de producto; fix RangeError en toBlob (solo incrustar imágenes ítem si son data URL, validación data URL del logo); fix aviso Next/Image en tabla presupuesto (width/height auto). Correo de acceso a la demo: plantilla HTML Veta (misma línea que magic_link) en `src/lib/email/templates/demo-access.ts`, botón con texto blanco inline. Campo `public_email` en account_settings (migración, personalización, PDF, seed). Viñeta/iluminación radial desactivada en `/veta-app` (clase `veta-app` en body, `body.veta-app::after { display: none }` en dark). Eliminación Cookiebot; gestión de cookies solo propia.
- **Rama fix/plan-actions (issue #120 + planes):** `Excluir del proyecto` pasa de `pdf_export_mode full` a `plus` (disponible desde Pro). Fix en creación de producto con imagen pesada: validación temprana cliente (máx. 5MB) en `AddItemDialog` y `ProductDialog`, rollback más robusto si falla `/api/upload/product-image`, y mensajes de error descriptivos para evitar el genérico "Error al guardar".
- **Rama feat/issues-122-123-124 (issues #122, #123, #124):** Presupuesto y vista pública de productos dejan de mostrar SKU/referencia de catálogo al cliente y muestran `internal_reference` cuando existe (`Cód.`). El formulario de ítems (`AddItemDialog`) ahora guarda/edita la descripción a nivel `project_items.description` (detalle por proyecto) sin sobrescribir la descripción del producto en catálogo. Migración SQL para actualizar `get_project_public_products` y exponer `internal_reference`.
- **Rama fix/issue-117-118-119 (issues #117, #118, #119):** Fix del desplegable de códigos telefónicos en modo oscuro (`PhoneInputCountrySelect` y `option` con colores de tema). En listados de Clientes y Catálogo, el card completo ahora es clicable para abrir el detalle/edición; se añadió soporte de teclado (Enter/Espacio) y `stopPropagation` en el menú de acciones para evitar aperturas accidentales.
- **i18n modular (common/marketing/auth) y fix language toggle:** Se reemplazó la carga monolítica `src/i18n/messages/{locale}.json` por carga modular en `src/i18n/request.ts` (merge de `messages/${locale}/common.json`, `marketing.json`, `auth.json`). Se crearon módulos ES/EN y se eliminaron `src/i18n/messages/es.json` y `en.json`. Se actualizaron imports dependientes de `PlanCopy` hacia `@/i18n/messages/es/marketing.json` (`src/lib/plan-copy.test.ts`, `src/app/veta-app/settings/plan/change/page.tsx`). `LanguageToggle` se robusteció normalizando paths y usando fallback sin prefijo de locale para evitar destinos incorrectos como `/en/precios` al cambiar desde `/en/pricing` (ES default con `localePrefix: "as-needed"`). Validado con `npm run format:check`, `npm test` y `npm run build`.
- **i18n Next.js 16 proxy + rutas ES:** Eliminado `middleware.ts` en raíz (Next 16 no permite `middleware.ts` y `proxy.ts` a la vez; el i18n no se aplicaba y las URLs ES localizadas daban 404 / not-found). `proxy.ts` exporta `default` para que el proxy ejecute rate limit, Supabase en rutas internas y `next-intl` en marketing/auth. Restaurados rewrites en `next.config.ts` que mapean slugs ES y planes EN a `src/app/[locale]/...`. `LanguageToggle` resuelve la URL canónica con `routing.pathnames` y navega con `window.location.href` para evitar prefijos duplicados del router de next-intl.
- **feat/i18n — bucle de redirección:** Eliminado el redirect permanente `/es/pricing` → `/precios` en `next.config.ts` para evitar un ciclo de redirección infinito con el enrutado i18n.
- **Rama fix/lang-toggle-translation (issue #133):** El menú del selector de tema en el header de marketing (`ThemeToggle`) usa `useTranslations("Common")` con claves `themeMenuLabel`, `themeLight`, `themeDark`, `themeSystem` en `src/i18n/messages/{en,es}/common.json`, de modo que en `/en` las opciones se muestran en inglés. Ajuste de URL en la sección de testimonios de la home (`home-testimonials-section.tsx`). Plan de trabajo i18n/SEO en `docs/i18n-seo-plan.md`. Skills de agente: formato Prettier en `veta-forms-validation` y `veta-supabase-rls`.
- **Rama feat/app-i18n-profile-lang — i18n /veta-app y /view-project sin URL:** Columnas `account_settings.lang` y `account_settings.date_format`; trigger `handle_new_user` crea `account_settings` con `lang` desde metadata; demo `lang=en`, `date_format=MM/DD/YYYY`. Layout `/veta-app` con `NextIntlClientProvider` + `AppFormattingProvider` (fechas según patrón, monedas/números según idioma). `/view-project/[token]` con layout que usa `Accept-Language` y `app.json` (ES/EN). Personalización: formulario idioma + formato de fecha; magic link fusiona `lang` (body o cabecera). Utilidades `src/lib/formatting.ts`, `currencies.ts`, tests `resolve-locale-from-accept-language`. Oleada 1: navegación app, vistas view-project, modal detalle proyecto, dashboard/proyectos/plan con `useAppFormatting`.
- **i18n consistente (app + view-project) y skill reforzada:** Se eliminaron hardcodes de estados, labels ARIA, fallback `General` y categorías/subcategorías de presupuesto en `view-project` y módulos de app (`project-budget`, `project-cost-control`, `project-dashboard`) usando `next-intl` con claves ES/EN. Se añadieron namespaces en `app-project-modules.json` y `app-view-project.json`, y se actualizó la skill `veta-multilanguage-views` para exigir anti-hardcode y evitar duplicación de etiquetas entre `app` y `view-project`.
- **Rama feat/auth-email-multilang — correos Auth y transaccionales ES/EN:** Plantillas HTML en `supabase/templates/` con bifurcación Go (`{{ .Data.lang }}`); asuntos bilingües en `supabase/config.toml`; `resolveEmailLocale` + `user_metadata.lang` vía magic-link, demo y contacto (`form_locale`); sincronización de `lang` en Auth al cambiar email o idioma en personalización; plantilla demo MailerSend bilingüe; `.prettierignore` para `supabase/templates`; doc `docs/supabase-email-templates.md` actualizada. Tests: `auth-email-lang`, `demo-access`, contacto EN, demo-request EN. **Endurecimiento:** `escapeHtml` compartido (`src/lib/escape-html.ts`) en correo interno demo-request y contacto; atributo `<html lang='…'>` con comillas simples en plantillas Supabase para no romper el HTML fuente con literales Go `"en"`.
- **Rama feat/anti-spam-blocking — magic link anti-spam y Turnstile:** Paquete reutilizable `src/lib/anti-spam/` (heurística local del email, verificación servidor Cloudflare Turnstile, resolver de flujo magic-link sin leer `process.env` dentro del paquete). Host: `src/lib/auth/magic-link-anti-spam-config.ts`; API `POST /api/auth/magic-link` admite `captchaToken`, acciones `captcha_required` / `silent_block` (tras captcha válido envía OTP) / `hard_block` / `flag_for_review` con metadata. Cookie HttpOnly firmada `veta_ml_cap` tras OTP exitoso en emails marcados sospechosos para permitir reenvío sin token Turnstile de un solo uso (opcional `MAGIC_LINK_CAPTCHA_BYPASS_SECRET`). Formularios sign-in/sign-up con `CaptchaGuard`, i18n `auth`; CSP en `next.config.ts` (`challenges.cloudflare.com`). Tests Vitest para resolve, bypass, rutas magic-link y headers.
- **Rama feat/signup-confirmation-profile-and-demo-seed:** Confirmación de email en local (`enable_confirmations = true`); perfil y `account_settings` solo tras confirmar (migración única con `ensure_profile_and_account_settings_from_auth`, `handle_new_user` / `handle_user_confirmed` y trigger `on_auth_user_confirmed`). Plantilla `confirmation.html` con bienvenida, beneficios y enlace a contacto (`/contacto`, `/en/contact`). Doc de flujo signup vs magic link. `npm run seed:demo` + `scripts/run-seed-demo-local.mjs` (ambas env `SUPABASE_URL` + `SUPABASE_SERVICE_ROLE_KEY` o ninguna vía `supabase status`); devDependency `tsx`. Ajustes ESLint/React Compiler (p. ej. `handleSubmit` en `onSubmit`, `incompatible-library` off), dashboard `useCallback`, validación de locale con `Set<string>` para build TS.
- **Rama feat/veta-blog-module (Veta.blog / issue #90):** Blog estático en Markdown bajo `src/content/blog/{es,en}/` con frontmatter (`title`, `date`, `excerpt`, `slug`, `coverImage`, `entryId` para parejas ES/EN). Rutas `src/app/[locale]/(marketing)/blog/` (índice + `[slug]`), SSG con `generateStaticParams`, HTML vía `gray-matter` + `remark`/`remark-html`. i18n: claves `Blog` + enlaces en `Header`/`Footer` en `marketing.json`; `routing.pathnames` `/blog` y `/blog/[slug]`; rewrites/redirects `/blog` ↔ `/es/blog` como el resto del marketing. Sitemap asíncrono mezcla rutas fijas + entradas del blog (`src/lib/blog-data.ts` para no arrastrar remark al sitemap). Middleware: `/blog` y `/blog/*` públicos. Tipografía Markdown: `@tailwindcss/typography` en `globals.css`. Imágenes cover: dominio `images.unsplash.com` en `next.config.ts`. **Cambio de idioma en el blog:** `resolveBlogLocaleSwitchPath` + `GET /api/blog/locale-target` y `LanguageToggle` enlazan al slug traducido vía `entryId`; si no hay par, índice del blog en el locale destino. Tests: `src/lib/blog.test.ts`. Contenido ampliado SEO/GEO; skills de agente para artículos e investigación de blog; `next.config`: Cache-Control largo para `/_next/static` solo en producción.
- **Blog — Veta vs hojas de cálculo (issue #78) y tablas GFM:** Entradas ES/EN comparando presupuestos en Excel con Veta (fases, partidas, productos, órdenes de compra, pagos vinculados a ÓC/costes adicionales, PDF, vista pública por token). Slugs: `veta-vs-excel-presupuestos-interiorismo` y `veta-vs-spreadsheets-interior-design-budgets`, mismo `entryId` para el toggle de idioma. El pipeline `markdownToHtml` usa `remark-gfm` y `remark-html` (`sanitize: false`) para que las tablas Markdown se rendericen como HTML; dependencia `remark-gfm` en el proyecto.
- **Blog — Vista pública para clientes:** Entradas ES/EN (abril 2026) sobre el beneficio de un portal de solo lectura por proyecto: renders y espacios, productos y estado de pedidos, costes de obra sin partidas internas, pagos registrados; CTA a demo y Plan Base. Slugs: `vista-publica-cliente-interiorismo` y `client-public-view-interior-design`, `entryId` `client-public-view`.
- **Blog — Sanitización del pipeline Markdown y tests:** `markdownToHtml` (`src/lib/blog-markdown.ts`) deja de usar `sanitize: false` y aplica el esquema por defecto de `hast-util-sanitize` vía `remark-html`, manteniendo tablas GFM (`<table>`, `<thead>`, `<tbody>`, `<tr>`, `<th>`, `<td>`) y bloqueando `<script>`, `<iframe>`, manejadores `onclick=…` y URLs `javascript:` (necesario porque `contentHtml` se inyecta con `dangerouslySetInnerHTML` en `blog/[slug]`). Nuevo archivo `src/lib/blog-markdown.test.ts` con 10 casos: párrafo + énfasis, trim de saltos, tabla GFM con conteos exactos de filas/columnas, strikethrough (`<del>`), autolinks GFM, eliminación de `<script>`/`<iframe>`, stripping de event handlers, neutralización de `javascript:` URL y entidades `&lt;script&gt;` que se mantienen escapadas.

## Pendiente

- [ ] **Dashboard — Total de Ingresos este mes:** Calcular el KPI desde la tabla `payments` (filtro por `payment_date` en el mes actual). El registro de pagos por proyecto ya existe; solo falta conectar el dashboard. Ver issue #61.
- [ ] **Notificaciones por email:** Avisos a clientes/proveedores (p. ej. vía Edge Functions).

---

*El historial detallado de cada feature (bugs del primer review, mejoras UI, etc.) está en el historial de git y en PRs cerrados.*
