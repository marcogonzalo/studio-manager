# ---------------------------------------------------------------------------
# Copy this file to .env.local and replace values with your real credentials.
# .env.local is not committed to the repository.
# ---------------------------------------------------------------------------

# Supabase (required)
NEXT_PUBLIC_SUPABASE_URL=https://xxxxxxxxxxxx.supabase.co

# Local development: Use Anon Key
NEXT_PUBLIC_SUPABASE_ANON_KEY=NEXT_PUBLIC_SUPABASE_ANON_KEY

# Production only: Publishable Key (client-side, safe with RLS) and Secret Key (server-side)
# NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY
# SUPABASE_SECRET_KEY=SUPABASE_SECRET_KEY

# ---------------------------------------------------------------------------
# Optional: MailerSend (contact form)
# Without MAILERSEND_API_KEY, the form will show a message to contact via email.
# Get your API key from: https://app.mailersend.com/tokens
# ---------------------------------------------------------------------------
# MAILERSEND_API_KEY=mlsn.xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
# CONTACT_EMAIL_TO=CONTACT_EMAIL_TO
# CONTACT_EMAIL_FROM=Veta Team <hey@veta.pro>

# ---------------------------------------------------------------------------
# Optional: Backblaze B2 (product images)
# Create Application Key at: https://secure.backblaze.com/app_keys.htm
# The bucket must be public for images to display.
# ---------------------------------------------------------------------------
# B2_APPLICATION_KEY_ID=xxxxxxxxxxxxxxxxxxxxxxxxxxxx
# B2_APPLICATION_KEY=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
# B2_BUCKET_ID=xxxxxxxxxxxxxxxxxxxxxxxxxxxx
# B2_BUCKET_NAME=your-bucket-name

# ---------------------------------------------------------------------------
# Optional: Base URL (sitemap, robots, metadata, Open Graph)
# On Vercel, VERCEL_URL is used by default. Local: https://veta.pro
# ---------------------------------------------------------------------------
# NEXT_PUBLIC_APP_URL=https://veta.pro
